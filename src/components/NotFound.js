import React from 'react';
import { render } from 'react-dom';

export default class NotFound extends React.component {
  render(
    return (
      <div>
        <h1> Sorry this page is not found. </h1>

        <p> add a link here to go back to start </p>

      </div>
    )
  )
}
